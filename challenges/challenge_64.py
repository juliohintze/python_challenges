# Write a function that receives a list of numbers and returns
# a dictionary containing two entries: the odd numbers and the
# even numbers.
# Example: if you receive [1, 2, 3, 4], it should return
# { "even": [2, 4], "odd": [1, 3] }
